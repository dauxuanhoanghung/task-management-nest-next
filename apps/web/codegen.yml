overwrite: true
schema: "http://localhost:8080/graphql"
documents: "gql/**/*.graphql"
generates:
  gql/api.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
    hooks:
      afterOneFileWrite:
        - prettier --write
